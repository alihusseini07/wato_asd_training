map_memory_node:
  ros__parameters:
    resolution: 0.1        # meters per cell
    width: 400             # cells (40m)
    height: 400            # cells (40m)
    origin_x: -20.0        # world coord of cell (0,0)
    origin_y: -20.0
